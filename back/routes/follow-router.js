const router = require('express').Router();
const Follow = require('../models').follow;

//Create GET route to retrieve a specific users follows
//Create GET route to retrieve a specific users followers


module.exports = router;
